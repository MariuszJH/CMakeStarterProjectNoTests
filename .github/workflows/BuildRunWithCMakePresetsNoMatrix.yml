name: Build and run with CMakePresets.json, no parameter matrix

on:
  push: 
    branches: [master, develop]
  pull_request:
    branches: [master, develop]

jobs:

  build-on-ubuntu:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repo
      uses: actions/checkout@v4

    - name: Install Ninja
      uses: seanmiddleditch/gha-setup-ninja@master

    - name: Tool versions
      shell: bash
      run: | 
        echo g++ --version
        g++ --version
        echo 
        echo clang++ --version
        clang++ --version
        echo 
        echo cmake --version
        cmake --version
        echo 
        echo ninja --version
        ninja --version

    - name: Linux, Ninja, Gnu, Debug
      run: |
        echo CMake configure
        cmake --preset Ninja-Gnu-Debug
        echo 
        echo CMake build
        cmake --build --preset Ninja-Gnu-Debug
        echo 
        echo CMake run
        cmake --build --preset Ninja-Gnu-Debug --target run

    - name: Linux, Ninja, Gnu, Release
      run: |
        echo CMake configure
        cmake --preset Ninja-Gnu-Release
        echo 
        echo CMake build
        cmake --build --preset Ninja-Gnu-Release
        echo 
        echo CMake run
        cmake --build --preset Ninja-Gnu-Release --target run

    - name: Linux, Ninja, Clang, Debug
      run: |
        echo CMake configure
        cmake --preset Ninja-Clang-Debug
        echo 
        echo CMake build
        cmake --build --preset Ninja-Clang-Debug
        echo 
        echo CMake run
        cmake --build --preset Ninja-Clang-Debug --target run

    - name: Linux, Ninja, Clang, Release
      run: |
        echo CMake configure
        cmake --preset Ninja-Clang-Release
        echo 
        echo CMake build
        cmake --build --preset Ninja-Clang-Release
        echo 
        echo CMake run
        cmake --build --preset Ninja-Clang-Release --target run

    - name: Linux, NinjaMC, Gnu, Debug
      run: |
        echo CMake configure
        cmake --preset NinjaMC-Gnu-Debug
        echo 
        echo CMake build
        cmake --build --preset NinjaMC-Gnu-Debug
        echo 
        echo CMake run
        cmake --build --preset NinjaMC-Gnu-Debug --target run

    - name: Linux, NinjaMC, Gnu, Release
      run: |
        echo CMake configure
        cmake --preset NinjaMC-Gnu-Release
        echo 
        echo CMake build
        cmake --build --preset NinjaMC-Gnu-Release
        echo 
        echo CMake run
        cmake --build --preset NinjaMC-Gnu-Release --target run

    - name: Linux, NinjaMC, Clang, Debug
      run: |
        echo CMake configure
        cmake --preset NinjaMC-Clang-Debug
        echo 
        echo CMake build
        cmake --build --preset NinjaMC-Clang-Debug
        echo 
        echo CMake run
        cmake --build --preset NinjaMC-Clang-Debug --target run

    - name: Linux, NinjaMC, Clang, Release
      run: |
        echo CMake configure
        cmake --preset NinjaMC-Clang-Release
        echo 
        echo CMake build
        cmake --build --preset NinjaMC-Clang-Release
        echo 
        echo CMake run
        cmake --build --preset NinjaMC-Clang-Release --target run


  build-on-macos:
    runs-on: macos-latest
    steps:
    - name: Checkout repo
      uses: actions/checkout@v4

    - name: Install Ninja
      uses: seanmiddleditch/gha-setup-ninja@master

    - name: Tool versions
      shell: bash
      run: | 
        echo g++ --version
        g++ --version
        echo 
        echo clang++ --version
        clang++ --version
        echo 
        echo cmake --version
        cmake --version
        echo 
        echo ninja --version
        ninja --version

    - name: MacOS, Ninja, Clang, Debug
      run: |
        echo CMake configure
        cmake --preset Ninja-Clang-Debug
        echo 
        echo CMake build
        cmake --build --preset Ninja-Clang-Debug
        echo 
        echo CMake run
        cmake --build --preset Ninja-Clang-Debug --target run

    - name: MacOS, Ninja, Clang, Release
      run: |
        echo CMake configure
        cmake --preset Ninja-Clang-Release
        echo 
        echo CMake build
        cmake --build --preset Ninja-Clang-Release
        echo 
        echo CMake run
        cmake --build --preset Ninja-Clang-Release --target run

    - name: MacOS, NinjaMC, Clang, Debug
      run: |
        echo CMake configure
        cmake --preset NinjaMC-Clang-Debug
        echo 
        echo CMake build
        cmake --build --preset NinjaMC-Clang-Debug
        echo 
        echo CMake run
        cmake --build --preset NinjaMC-Clang-Debug --target run

    - name: MacOS, NinjaMC, Clang, Release
      run: |
        echo CMake configure
        cmake --preset NinjaMC-Clang-Release
        echo 
        echo CMake build
        cmake --build --preset NinjaMC-Clang-Release
        echo 
        echo CMake run
        cmake --build --preset NinjaMC-Clang-Release --target run


  build-on-windows:
    runs-on: windows-latest
    steps:
    - name: Checkout repo
      uses: actions/checkout@v4

    - name: Setup MSVC compiler
      uses: ilammy/msvc-dev-cmd@v1

    - name: Install Ninja
      uses: seanmiddleditch/gha-setup-ninja@master

    - name: Tool versions
      shell: bash
      run: | 
        echo cl
        cl
        echo 
        echo cmake --version
        cmake --version
        echo 
        echo ninja --version
        ninja --version

    - name: Windows, Ninja, Msvc, Debug
      run: |
        echo CMake configure
        cmake --preset Ninja-Msvc-Debug
        echo 
        echo CMake build
        cmake --build --preset Ninja-Msvc-Debug
        echo 
        echo CMake run
        cmake --build --preset Ninja-Msvc-Debug --target run

    - name: Windows, Ninja, Msvc, Release
      run: |
        echo CMake configure
        cmake --preset Ninja-Msvc-Release
        echo 
        echo CMake build
        cmake --build --preset Ninja-Msvc-Release
        echo 
        echo CMake run
        cmake --build --preset Ninja-Msvc-Release --target run

    - name: Windows, NinjaMC, Msvc, Debug
      run: |
        echo CMake configure
        cmake --preset NinjaMC-Msvc-Debug
        echo 
        echo CMake build
        cmake --build --preset NinjaMC-Msvc-Debug
        echo 
        echo CMake run
        cmake --build --preset NinjaMC-Msvc-Debug --target run

    - name: Windows, NinjaMC, Msvc, Release
      run: |
        echo CMake configure
        cmake --preset NinjaMC-Msvc-Release
        echo 
        echo CMake build
        cmake --build --preset NinjaMC-Msvc-Release
        echo 
        echo CMake run
        cmake --build --preset NinjaMC-Msvc-Release --target run
